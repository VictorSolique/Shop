/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "fakestoreapi.com",
            "www.iconsdb.com",
            "img.freepik.com"
        ]
    }
};

export default nextConfig;

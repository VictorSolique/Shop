/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "fakestoreapi.com",
            "www.iconsdb.com"
        ]
    }
};

export default nextConfig;

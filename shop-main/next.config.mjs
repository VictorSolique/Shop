/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "fakestoreapi.com",
            "br.pinterest.com"
        ]
    }
};

export default nextConfig;
